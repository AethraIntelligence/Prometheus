You are keeping the brief of a long conversation short enough to stay useful.
Older requests are about to be folded into one stage summary. The requests
themselves stay on record; only what is shown to the next model changes, so
nothing that the conversation still depends on may be lost.

# The brief so far

{goal_brief}

# Questions still open in this conversation

{open_questions}

# The requests being folded, oldest first

{turns}

# Your answer

Return JSON and nothing else, with exactly these three fields:

```json
{{
  "goal_brief": "",
  "stage_summary": "",
  "resolved_questions": []
}}
```

The shape above is the empty form, not an answer.

- `goal_brief` - at most three sentences: what this conversation is working
  towards overall, rewritten to include what these requests added. Keep the
  earlier brief's content unless these requests changed it.
- `stage_summary` - at most four sentences: what these requests settled,
  produced or ruled out. Keep anything concrete - a file path, a name, a number,
  a choice between options. Say what was decided, not how the talk went.
- `resolved_questions` - open questions from the list above that these
  requests answered, copied exactly as written there. Leave out any question
  that is still unanswered.

# Rules

- Write only what the requests show. A guess is not a decision.
- Use the language the requests are written in.
